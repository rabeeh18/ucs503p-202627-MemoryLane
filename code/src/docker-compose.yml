version: '3.8'
services:
  solr:
    image: solr:9.7
    container_name: memorylane-solr
    ports:
      - "8983:8983"
    volumes:
      - solr_data:/var/solr
    command: solr-precreate memorylane
    restart: unless-stopped

volumes:
  solr_data:
